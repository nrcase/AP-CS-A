public class Rectangle
{
    private int side;

    public Rectangle(int s) //default constructor for rectangle objects, gets side length
    {
        side = s;
    }

    public int getLength() //gets the value stored inside the Rectangle object
    {
        return side;
    }
}
